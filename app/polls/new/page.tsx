import PollForm from '@/components/polls/PollForm'

export default function NewPollPage() {
  return <PollForm />
}

export const metadata = {
  title: 'Create New Poll | Polling App',
  description: 'Create a new poll and start collecting responses',
}